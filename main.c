//
// Created by jan on 31.10.22.
//

#include <stdio.h>
#include "recursive_descent.h"
#include "precedent.h"
#include "scanner.h"

int main(void) {
    //int result = analyse_syntax(stdin);
    /*Token token;
    init_str(&token.val);
    get_token(stdin, &token);
    struct tree_node *node = init_tree_node();
    add_tn_data(node, EXPRESSION, "Expression");
    int result = analyse_precedent(stdin, &token, node, 1);
    printf("%i\n", result);
    return result;*/
    return 0;
}
